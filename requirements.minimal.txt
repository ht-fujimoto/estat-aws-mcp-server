# 最小限の依存関係（本番環境用）
# 不要なパッケージを削除してイメージサイズを削減

# 必須パッケージ
requests>=2.28.0,<3.0.0
boto3>=1.26.0,<2.0.0
aiohttp>=3.9.0,<4.0.0
mcp>=1.0.0,<2.0.0

# データ処理（必要な場合のみ）
# pandas と pyarrow は大きいので、本当に必要な場合のみインストール
# pandas>=1.5.0,<2.0.0
# pyarrow>=10.0.0,<11.0.0

# asyncioは標準ライブラリなので不要
